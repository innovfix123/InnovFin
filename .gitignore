# Dependencies
node_modules/

# Next.js / build
/.next/
/out/
/build/
/dist/
next-env.d.ts
*.tsbuildinfo

# Secrets / env — NEVER commit
.env
.env*.local
*.key
*.pem

# Confidential finance data — NEVER commit (lives here)
/_private/
/data/
/.data/
/uploads/
# Defense-in-depth: never commit raw financial workbooks anywhere
*.xlsx
*.xls

# Local database
*.sqlite
*.sqlite3
prisma/*.db
prisma/*.db-journal

# Logs
*.log
npm-debug.log*
yarn-debug.log*
# MCP endpoint access/audit logs (records of who queried creator PANs / gateway fees — never commit)
/OnlyCare-TDS-mcp/logs/
/Hima-TDS-mcp/logs/
/Gateway-Settlements-mcp/logs/
/invoice-intelligence/logs/
/Drive-mcp/logs/

# Google Drive MCP: service-account JSON key (read/write access to the finance folder) — NEVER commit.
/secrets/
*-sa.json

# Local working directories: planning docs mixed with screenshots, raw workbooks, invoice PDFs,
# and live API credentials pasted during build sessions. Never auto-add — review file by file
# before anything from here is intentionally committed. (Source of truth lives in src/mcp/*.)
/Hima-TDS-mcp/
/OnlyCare-TDS-mcp/
/tds-working-mcp/
# Only its logs/ was ignored, so build-session screenshots sitting alongside (GCP consent screens,
# OAuth client ids, console dialogs) were still stageable into this PUBLIC repo. Ignore it wholesale
# like its siblings above.
/Gateway-Settlements-mcp/

# Screenshots pasted into the repo root during build sessions — never part of the source.
/image.png
/image copy*.png

# GSTR-2B working directory: 609 MB of raw PhonePe/Cashfree merchant settlement exports (.zip/.csv,
# every customer payment for the month), the GSTR-1 working .xlsx, and build-session screenshots.
# Customer payment data and statutory workings — never commit, and this repo is PUBLIC.
/GSTR-2B-est-mcp/

# Reconciliation anchors: revenue totals, filed-return figures and real order ids. The code that
# reads them is public; the numbers are not. See src/lib/recon/check-june.ts.
*.local.json

# invoice-intelligence Python service: venv + runtime state (captured invoice data, blobs, SQLite
# store, dedup/registry ledgers, audit JSONL) — source is tracked, runtime state is NEVER committed.
/invoice-intelligence/.venv/
/invoice-intelligence/build/

# OS / editor
.DS_Store
.idea/
.vscode/
